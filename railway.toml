[build]
builder = "nixpacks"
watch = ["rakshak"]

[deploy]
startCommand = ". /opt/venv/bin/activate && cd rakshak && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
